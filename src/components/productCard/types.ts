export interface Props {
  topLabel?: string;
  bottomLabel?: string;
  brand: string;
  name: string;
  shortDescription: string;
  price: number;
  priceWithSale?: number;
  imageUrl: string;
  id: string;
}
