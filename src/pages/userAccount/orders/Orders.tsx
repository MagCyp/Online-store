import { FC, useState } from 'react';

import { Props } from '@pages/userAccount/orders/types';

const Orders: FC<Props> = ({ content }) => {
  return <div>{content}</div>;
};

export default Orders;
